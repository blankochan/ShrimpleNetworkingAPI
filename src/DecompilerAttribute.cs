public sealed class DecompilerComment : Attribute
{

    public const string comment = "doing this purely to give comments to people using a decompiler";
    public DecompilerComment(string comment) { }
}
